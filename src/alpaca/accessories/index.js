const ACCESSORIZES = {
  earings: require('./earings.png').default,
  flower: require('./flower.png').default,
  glasses: require('./glasses.png').default,
  headphone: require('./headphone.png').default
}

export default ACCESSORIZES
